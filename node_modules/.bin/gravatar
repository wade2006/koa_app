#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_gravatar@1.8.1@gravatar/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../_gravatar@1.8.1@gravatar/cli.js" "$@"
  ret=$?
fi
exit $ret
